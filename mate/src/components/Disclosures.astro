---
const { content } = Astro.props;
---
<div class="py-16 max-w-4xl px-6 md:px-12 mx-auto" id="faq-section">
    <h2 class="my-4 text-center font-bold text-gray-700 dark:text-white md:text-4xl">
      Frequently asked questions
    </h2>
    <div class="space-y-4 py-10">
      {
        content.map(item => (
          <details class="bg-gray-100 dark:bg-gray-800 dark:text-white rounded-lg border-1 border-gray-500 dark:border-gray-200">
              <summary class="font-medium p-6 cursor-pointer " set:html={item.question}></summary>
              <p class="p-6 pt-2 text-gray-800 dark:text-gray-400" set:html={item.answer}></p>
          </details>
        ))
      }
    </div>
</div>